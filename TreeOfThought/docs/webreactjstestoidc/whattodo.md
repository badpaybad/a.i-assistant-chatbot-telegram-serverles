cần làm 1 app reactjs hoàn toàn độc lập chạy không cần server vào folder TreeOfThought/frontend/webreactjstestoidc
các yêu cầu để test
    - login, logout oidc SSO vào http://localhost:5000
    - trang home giới thiệu về việc login, logout oidc sso với tài khoản test admin / admin123
    - cần 1 trang khi vào nếu chưa login sẽ bị báo bạn chưa login rồi redirect về trang home 
        - ở trang này sẽ lấy thông tin user và hiển thị 
        - cho phép logout