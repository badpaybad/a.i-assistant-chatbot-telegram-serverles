tạo hoặc cập nhật file

sort.py

bên trong có hàm sort nhận vào 1 list object và 1 lambda return -1 0 1

viết unit test cho hàm sort


Tôi không muốn dùng các thuật toán sẵn có hay thư viện sẵn có, hãy tự tạo ra thuật toán cho riêng mình

tạo ra code inside hàm sort và unit test cho đến khi nào pass hết unit test thì dừng và chạy nhanh nhất có thể, kể cả dùng tới L3 cache của CPU và các kỹ thuật tối ưu khác

Bạn tự tạo các test để kiểm tra CPU RAM và tốc độ thực thi của hàm sort, lưu kết quả và lấy ra hàm có code tối ưu nhất 